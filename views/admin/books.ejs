<%- include('../includes/header.ejs') %>
<%- include('../includes/navigation.ejs') %>


<main>
    <div class="row mt-3">
        <% if(books.length > 0) { %>
            <% for(let book of books) { %>
                <div class="col-4">
                    <div class="card" style="width: 18rem;">
                        <img class="card-img-top" src="<%- book.imageUrl %>" alt="<%= book.title %>">
                        <div class="card-body">
                            <h5 class="card-title"><%= book.title %></h5>
                            <p class="card-text"><%= book.description %></p>
                            <h5 class="card-text"><%= book.price %> €</h5>
                            <a href="/admin/edit-book/<%= book.id %>?edit=true" class="btn btn-succes">Editer le livre</a>
                            <form action="/admin/delete-book" method="POST">
                                <input type="hidden" value="<%= book.id %>" name="bookId">
                                <button type="submit" class="btn btn-danger">Supprimer</a>
                            </form>
                        </div>
                    </div>
                </div>
                <% } %>
            <% } %>
    </div>
</main>



<!-- <main>
    <% if(books.length > 0) { %>
    <div class="grid">
        <% for(let book of books) { %>
        <article class="card book-item">
            <header class="card__header">
                <h1 class="book__title"><%= book.title %></h1>
            </header>
            <div class="card__image">
                <img src="<%- book.imageUrl %>" alt="<%= book.title %>">
            </div>
            <div class="card__content">
                <h2 class="book__price"><%= book.price %> €</h2>
                <p class="book__description"><%= book.description %></p>
            </div>
            <div class="card__actions">
                <a href="/admin/edit-book" class="btn">Editer le livre</a>
                <form action="/admin/delete-book" method="POST">
                    <button class="btn" type="submit">Supprimer</button>
                </form>
            </div>
        </article>
        <% } %>
    </div>
    <% } %>
</main> -->


<%- include('../includes/footer.ejs') %>